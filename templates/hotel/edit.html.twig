{% extends 'base.html.twig' %}

{% block title %}Modifier {{ hotel.name }}{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier {{ hotel.name }}</h1>

        {{ include('hotel/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('app_hotel_index') }}" class="btn btn-lg btn-primary" style="margin-top:1%">Revenir à la liste d'hôtel</a>
    </div>
{% endblock %}
